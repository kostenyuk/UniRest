
&НаСервере
Процедура ПриЧтенииНаСервере(ТекущийОбъект)
	
	Если ТекущийОбъект.Предопределенный Тогда
		Элементы.Тип.Вид = ВидПоляФормы.ПолеНадписи;
		Элементы.ВидОбъектаКонтактнойИнформации.Вид = ВидПоляФормы.ПолеНадписи;
	КонецЕсли; 
	
КонецПроцедуры // ПриЧтенииНаСервере()
