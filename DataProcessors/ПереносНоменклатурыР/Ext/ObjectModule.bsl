Процедура ПереносНоменклатуры(Источник, ПометкаУдаления=Неопределено) Экспорт
	
	Если ТипЗнч(Источник)=Тип("Форма") Тогда
		
		Форма = ПолучитьФорму("ОсновнаяФорма",Источник);
		Форма.Источник = Источник.ДокументОбъект;
		Форма.ФормаИсточника = Источник;
		
	Иначе
		
		Форма = ПолучитьФорму("ОсновнаяФорма");
		Форма.Источник = Источник;
		
	КонецЕсли; 
	
	Форма.ОткрытьМодально();

КонецПроцедуры
 