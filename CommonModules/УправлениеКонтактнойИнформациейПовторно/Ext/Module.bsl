
Функция ПолучитьШаблоныТелефонныхНомеров() Экспорт
	
	УстановитьПривилегированныйРежим(Истина);
	
	СтруктураШаблонов = Константы.ШаблоныТелефонныхНомеров.Получить().Получить();
	Если (Не ТипЗнч(СтруктураШаблонов) = Тип("Соответствие")) Тогда
		СтруктураШаблонов = Новый Соответствие;
	КонецЕсли;
	
	УстановитьПривилегированныйРежим(Ложь);
	
	Возврат СтруктураШаблонов;
	
КонецФункции // ПолучитьШаблоныТелефонныхНомеров()
