
Функция НайтиПоНоменклатуре(Номенклатура) Экспорт
	
	// Проверка.
	Если (ТипЗнч(Номенклатура) = Тип("СправочникСсылка.УсловияНоменклатуры")) Тогда
		Возврат Номенклатура;
	КонецЕсли; 
	
	// Поиск.
	Возврат Справочники.УсловияНоменклатуры.НайтиПоРеквизиту("Номенклатура", Номенклатура);

КонецФункции // НайтиПоНоменклатуре()


Процедура ПеренестиНоменклатуру(Номенклатура, ПараметрыПереноса) Экспорт
	
	Справочники.Меню.ПеренестиНоменклатуру(Номенклатура, ПараметрыПереноса, "УсловияНоменклатуры");
	
КонецПроцедуры // ПеренестиНоменклатуру()
