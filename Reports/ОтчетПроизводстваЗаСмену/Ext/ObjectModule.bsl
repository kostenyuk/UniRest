
Процедура ПриКомпоновкеРезультата(ДокументРезультат, ДанныеРасшифровки, СтандартнаяОбработка)
	
	ОбластьКолВо = ДокументРезультат.НайтиТекст("Кол во");
	
	Если НЕ ОбластьКолВо = Неопределено Тогда
		
		ВерхЗаголовка = ОбластьКолВо.Верх + 1;
		НизЗаголовка = ОбластьКолВо.Низ + 1;
		
		ДокументРезультат.УдалитьОбласть(ДокументРезультат.Область("R"+Строка(ВерхЗаголовка)+":R"+Строка(НизЗаголовка)),ТипСмещенияТабличногоДокумента.ПоВертикали);
		
	КонецЕсли;
	
КонецПроцедуры
