
Функция ПереносНоменклатуры(ДокументОбъект, ПервоночальнаяПометка = Ложь, РасширеннаяНастройка = Ложь) Экспорт
	
	// Форма выбора.
	Форма = ПолучитьФорму("Форма");
	Форма.ДокументОбъект = ДокументОбъект;
	Форма.ПервоночальнаяПометка = ПервоночальнаяПометка;
	Форма.РасширеннаяНастройка = РасширеннаяНастройка;
	
	// Выбор.
	Значение = Форма.ОткрытьМодально();
	
	// Результат.
	Возврат (Значение = Истина);

КонецФункции // ПереносНоменклатуры()
 