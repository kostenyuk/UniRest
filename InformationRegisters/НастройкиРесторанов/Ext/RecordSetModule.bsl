
// Обработчик события ПередЗаписью объекта.
//
Процедура ПередЗаписью(Отказ, Замещение)
	
	// При обмене данными ничего не проверяем.
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
	// Нормализация.
	Для Каждого Запись Из ЭтотОбъект Цикл
		Запись.ПродолжительностьДоставки = Макс(Запись.ПродолжительностьДоставки, '00010101000500');
		Запись.ПродолжительностьПроизводства = Макс(Запись.ПродолжительностьПроизводства, '00010101000500');
	КонецЦикла;
	
КонецПроцедуры // ПередЗаписью()
